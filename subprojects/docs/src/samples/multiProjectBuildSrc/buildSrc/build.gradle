subprojects {
  apply plugin: "groovy"

  dependencies {
    api localGroovy()
      api gradleApi()
  }

// tag::addToRootProject[]
rootProject.dependencies {
  runtime project(path)
}
// end::addToRootProject[]
}
